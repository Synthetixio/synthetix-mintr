import React from 'react';
import styled from 'styled-components';

const Landing = () => {
  return <LandingWrapper>this is the landing page</LandingWrapper>;
};

const LandingWrapper = styled.div``;

export default Landing;
