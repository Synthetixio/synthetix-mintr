import React from 'react';
import styled from 'styled-components';

const Home = () => {
  return <HomeWrapper>This is the home content</HomeWrapper>;
};

const HomeWrapper = styled('div')`
  margin-top: 100px;
`;

export default Home;
