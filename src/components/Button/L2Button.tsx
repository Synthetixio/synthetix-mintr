import React from 'react';
import styled from 'styled-components';

interface L2ButtonProps {
	onClick: Function;
}

export const L2Button: React.FC<L2ButtonProps> = ({ onClick }) => {
	return <></>;
};
